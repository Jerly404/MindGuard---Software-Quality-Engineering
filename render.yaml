services:
  # Backend
  - type: web
    name: mindguard-backend
    env: python
    region: ohio
    plan: free
    buildCommand: cd backend && pip install -r requirements.txt
    startCommand: cd backend && export PYTHONPATH=$PYTHONPATH:. && python reset_db_final.py && gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker -b 0.0.0.0:$PORT
    envVars:
      - key: DATABASE_URL
        value: sqlite+aiosqlite:///./mindguard.db
      - key: SECRET_KEY
        generateValue: true
      - key: GOOGLE_API_KEY
        sync: false
      - key: GROQ_API_KEY
        sync: false

  # Frontend
  - type: web
    name: mindguard-frontend
    env: static
    buildCommand: cd frontend && npm install && npx vite build
    staticPublishPath: ./frontend/dist
    envVars:
      - key: VITE_API_URL
        fromService:
          name: mindguard-backend
          type: web
          property: host
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
